apiVersion: v1
kind: Service
metadata:
  name: {{ include "mon-application.fullname" . }}
  labels:
    {{- include "mon-application.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mon-application.selectorLabels" . | nindent 4 }}
